package tests

import (
	"testing"
	// . "github.com/smartystreets/goconvey/convey"
)

func Test_file(t *testing.T) {
	//TODO fix this
	// Convey("The following path is passed to the file parser: C:\\foo/bar/file.ext", t, func() {
	// 	var path = "C:/foo/bar/file.name.ext:42 +23e2"
	// 	var parsed = utils.ParseFilePath(path)

	// 	So(parsed.Dir, ShouldEqual, "bar")
	// 	So(parsed.Name, ShouldEqual, "file")
	// 	So(parsed.Ext, ShouldEqual, "ext")
	// 	So(parsed.LineNumber, ShouldEqual, -1)
	// })

	// Convey("The following path is passed to the file parser: C:/foo/bar\\file.name.ext:42 +23e2", func() {
	// 	var path = "C:/foo/bar/file.name.ext:42 +23e2"
	// 	var parsed = utils.ParseFilePath(path)

	// 	So(parsed.Dir, ShouldEqual, "bar")
	// 	So(parsed.Name, ShouldEqual, "file.name")
	// 	So(parsed.Ext, ShouldEqual, "ext")
	// 	So(parsed.LineNumber, ShouldEqual, 42)
	// 	So(parsed.Full, ShouldEqual, "C:/foo/bar/file.name.ext")
	// 	So(parsed.Trail, ShouldEqual, " +23e2")
	// })
}
